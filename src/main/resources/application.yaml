server:
  port: 8090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sintern
    username: postgres
    password: P@trici@1
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL9Dialect